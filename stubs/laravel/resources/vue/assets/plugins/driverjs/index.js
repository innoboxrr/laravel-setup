import startDriver from './startDriver';
import initDashboardDriver  from './dashboardDriver';
import initChatDriver  from './chatDriver';

export default {
    Start: startDriver,
    Dashboard: initDashboardDriver ,
    Chat: initChatDriver ,
}